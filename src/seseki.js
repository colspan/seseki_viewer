// import React from "react"
import ReactDOM from "react-dom"
import "./components/main.jsx"

ReactDOM.render(
  <CommentBox message="and GoodBye World" date={new Date()} />,
  document.getElementById("app")
)
